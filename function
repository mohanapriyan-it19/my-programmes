#include <stdio.h> 
void main()
{
    int a,b;
    printf("enter the value");
    scanf("%d%d",&a,&b);
    if(a>b)
     printf("%d is a greatest value",a);
     else 
      printf("%d is a greatest value",b);
}
